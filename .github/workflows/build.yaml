name: Build Demo
on: [push]
jobs:
    Build:
        runs-on: ubuntu-20.04
        steps:
            - uses: actions/checkout@v3
            - run: mkdir build && cd build
                   cmake ../
                   make -$(nproc)
